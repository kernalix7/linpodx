//! Workspace-root integration-test crate (`linpodx-phase17-integration`).
//!
//! Real scenarios live in `tests/phase17_integration.rs` (cargo's standard
//! integration-test directory layout). The library itself is empty — it exists
//! only so the crate is a valid workspace member with a discoverable
//! `tests/` directory.
