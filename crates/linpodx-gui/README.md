# linpodx-gui

Desktop GUI for linpodx.

**Status:** Placeholder (Phase 0). The actual implementation lands in Phase 1 using
[iced](https://iced.rs/) (pure Rust, MIT) for the dashboard, log viewer, and container
controls. Daemon access is via the Unix socket IPC defined in `linpodx-common`.
