//! Placeholder library so `bench-tools` is a valid workspace crate.
//!
//! Real benches live in `benches/`. The library exists only because cargo
//! requires a crate target for the package to be a workspace member.
